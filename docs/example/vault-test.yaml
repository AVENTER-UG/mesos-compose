version: '3.9'
services:
  test1:
    image: alpine
    restart: always
    network: default
    environment:
      - test=vault://secret/data/abd
    deploy:
      resources:
        limits:
          cpus: "0.1"
          memory: "50"

          
